rules:
  - id: fork-process
    languages:
      - php
    message: sample
    metadata:
      description: sample
      tags: LOW
    patterns:
      - pattern-either:
        - pattern: pcntl_fork(...)
        - patterns:
            - pattern: popen(..., $MODE)
            - metavariable-regex:
                metavariable: $MODE
                regex: ('|"")?r('|")?
        - pattern: fsockopen(...)
    severity: WARNING